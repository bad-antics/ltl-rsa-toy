// examples/demo.ltl — runnable demo for ltl_rsa_toy

import std::io
use ltl_rsa_toy as lib

fn main() {
    io::println("ltl_rsa_toy demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}